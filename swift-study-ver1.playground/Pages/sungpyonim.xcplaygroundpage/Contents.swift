
import Foundation



func test(){
    print("저는 진미래입니다.")
}
//익숙하게 보던 print도 함수입다.
//스위프트로 만든 누군가가 이미 만들어 둔 것이다.

func test_1() -> Strig{
    
}
//필요하다면 "->" 이런 화살표로 타입을 명시해줄 수 있다.
//괄호 안에는 매개변수라는 것이 들어간다.
//타입을 명시했다면 무조건 리턴이 있어야한다.

func hihi(good: Int, howAboutThis iDontKnow: Double, _bad:String)

print(good)
print(iDontKnow)
print(bad)
}

hihi(good:0, howAboutThis: 0,1, "배드!")

func plus(){
    exitNumber +=1
    print("함수")
    if exitNumber < 5 {
        plus()
    }
}

//self를 안써도 되나 언제 안써도 되는지. 블로그 포스팅
 
 
